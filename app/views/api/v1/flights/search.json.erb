<%=
     @flights.to_json(
        only: :id,
        include: {
                    airport_destination: { only: [:name, :country] },
                    airport_origin: { only: [:name, :country] }
                  }).html_safe
%>
